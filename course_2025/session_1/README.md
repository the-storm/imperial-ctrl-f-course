## Structrizr
 <!-- https://docs.structurizr.com/onpremises/building steps -->
**Building steps**
* `cd structurizr-onpremises`
* `./ui.sh`
* `./gradlew clean build`

## Web Image Gallery
Requires AMD Architecture
`DOCKER_DEFAULT_PLATFORM=linux/amd64 tilt up`
`DOCKER_DEFAULT_PLATFORM=linux/amd64 docker compose up`
## KalmarNotes
* `docker compose up`
* `tilt up`

## HedgeDoc
TODO